---
title: Runtime Hook
description: 深入理解 Runtime Hook 的生命周期、命令防护和 TODO 管理
sidebar:
  order: 3
---

import { Aside } from '@astrojs/starlight/components'

## Runtime Hook 概述

Runtime Hook 是 Skills Framework 的核心组件，在每次用户请求、工具调用、会话启动和结束时自动运行，提供：

- ✅ **行为内核注入**：跨客户端一致的行为原则
- ✅ **项目记忆加载**：自动读取相关上下文
- ✅ **TODO 自动管理**：任务领取、完成检查、续作
- ✅ **命令防护**：拦截高风险 Bash 命令
- ✅ **流程门禁**：状态机推进控制
- ✅ **语音提醒**：任务完成、需要确认时提醒

## Hook 生命周期

### SessionStart Hook

**触发时机**：客户端启动新会话时

**主要功能**：

1. **检查配置一致性**

比对客户端里已装的 Hook 配置和仓库当前版本。一致就静默通过；不一致会自动合并、去重，并在改写前备份原配置。

<Aside type="tip">
设置 `AGENT_RUNTIME_HOOKS_AUTO_UPDATE=0` 可关闭自动修复。
</Aside>

2. **检查 Penpot MCP 注册状态**

只看本地有没有注册，不连远端，所以不会因为网络问题拖慢会话启动。

```bash
claude mcp get penpot
```

未注册时会提示你运行安装器。

开发者身份和运行实例由上下文及 TODO 脚本在请求时解析。SessionStart 自检只处理托管配置和本地 Penpot 注册状态。

**安装器行为**：

```bash
node runtime-hooks/scripts/install.mjs
```

它会先备份现有客户端配置，再把各个 Hook 合并进去（重复项去重），已有的其他 Hook 不会被覆盖。

### UserPromptSubmit Hook

**触发时机**：用户每次提交输入时（在 AI 处理之前）

**主要功能**：

1. **注入行为内核**

每轮请求会自动注入一份跨客户端统一的行为准则，约束事实来源、改动范围和完成判据。你不需要手工维护、复述或在提示词里重复它，Claude Code 与 Codex 得到的是同一份。

2. **自动定位项目根目录**

从当前工作目录向上找 Git 根目录，作为项目根。多仓库任务应在各自的 Git 工作目录里读取对应记忆和 TODO。

<Aside type="caution">
不在 Git 仓库里时找不到项目根，项目记忆和 TODO 上下文都不会注入。
</Aside>

3. **确保项目记忆文件存在**

按仓库根 `PROJECT-MEMORY.md` → `docs/PROJECT-MEMORY.md` → `docs/project-memory.md` 的顺序查找。都没有就在根目录创建一份空模板；主要章节还是空的，会提示先用 `init-docs` 采集事实。

4. **加载相关项目记忆**

只取与当前问题相关的稳定事实（技术栈、复用地图、边界），不是整份 `PROJECT-MEMORY.md`。

<Aside type="caution">
注入的是摘要。需要完整背景时直接说"读取 PROJECT-MEMORY.md 的完整内容"。
</Aside>

5. **提示当前 TODO 状态**

如果当前会话已经领取了某个 Feature，这一轮会带上它的 ID、依赖、写集和验收条件，你不需要每轮重述"我在做哪个功能"。

如果还处在规划阶段，带上的是当前阶段、已完成阶段、下一阶段和阶段 Owner——以及当前阶段是否允许修改生产文件。

6. **提供辅助路由提示**

本地 `skill-router.yaml` 命中场景时可输出候选 Skill。这是辅助上下文，不代表已经调用 Skill；可用清单和正文仍由 Harness 原生发现按需加载。

**上下文预算控制**：注入内容有总量上限，行为内核、项目记忆和 TODO 提醒都只放当前问题真正需要的部分。超过预算时自动截断并提示直接读取文件。

### PreToolUse Hook

**触发时机**：AI 尝试调用安装器 matcher 覆盖的写入或命令工具之前（Write、Edit、Bash、exec_command、apply_patch 等）

**主要功能**：

1. **命令防护**

检查广泛目标的递归强制删除、强制推送或删除远端 ref、丢弃工作区状态、清库和根目录权限改写等命令。命中后返回拒绝原因；放行不等于操作安全或已授权。

验证安装不应执行危险命令。准确规则与只检查命令字符串的方法见 [PreToolUse Hook](/skills/hooks/pre-tool-use/)。`AGENT_COMMAND_GUARD=0` 会跳过整个 PreToolUse，包含流程写入门禁，不应当作排障修复。

2. **流程门禁检查**

规划阶段还没结束时，试图编辑生产代码会被拒绝，并告知当前阶段和需要先推进到哪个阶段。规划阶段仍然可以只读调查、写 `docs/` 下的规划文档，以及推进阶段本身。

**阶段权限表**：

| 阶段类型 | Read | Write 规划文档 | Write 生产代码 | Bash |
|------|------|--------------|--------------|------|
| 规划阶段 | ✓ | ✓ | ✗ | 只读 |
| 实施阶段 | ✓ | ✓ | ✓ | ✓ |
| 交付阶段 | ✓ | ✓ | ✗ | 状态核验及已授权 Git 交付 |

3. **TODO 和写集边界**

PreToolUse 会在活动工作流中拒绝直接修改 TODO，要求由状态机原子写回。写集互斥由 TODO 状态机在并行领取、状态解析和完成检查时核验；当前 PreToolUse 不逐文件比对并行写集，Agent 仍需核对实际 diff。

<Aside type="tip">
并行开发时，写集互斥检查防止两个任务同时改同一个文件。
</Aside>

### SubagentStart Hook

**触发时机**：启动子智能体时

**主要功能**：

与 UserPromptSubmit 复用同一上下文注入逻辑。具体委派还应由主 Agent 写清：

1. **父任务上下文**：主任务是什么、当前在做哪个 Feature。
2. **职责边界**：它只负责这个 Feature 的哪一部分、能改哪些文件；不允许改写集外的文件，也不允许自己变更 TODO 状态。
3. **交接契约**：结束时必须交回哪些东西——改了哪些文件、覆盖了哪些 AC、证据是什么。

<Aside type="tip">
并行委派必须由主 Agent 划定互斥写集；不能把上下文提示当作文件锁。
</Aside>

### SubagentStop Hook

**触发时机**：子智能体完成任务时

**主要功能**：

子智能体返回时校验交接契约是否完整——改动文件、覆盖的 AC 及其证据、执行过的命令、做过的决策、以及尚未解决的风险。缺项会被指出；带未解决风险时仍然允许交接，但风险会显式带回给主 Agent，不会被悄悄吞掉。

<Aside type="caution">
SubagentStop 只校验 handoff，不代替主 Agent 的 Review、完成切换或 Git 收口。
</Aside>

### Stop Hook

**触发时机**：会话结束或任务完成时

**主要功能**：

1. **完成检查**

回合结束时核对当前 Feature 是否真的可以标完成。任何一项不满足，Feature 就留在 `[~]`，并告诉你缺的是哪一项。

**完成门禁要求**：

- ✅ 所有 AC 都有证据（每条 AC 后面必须挂上真实的验证结果，不是"应该没问题"）
- ✅ Review: pass
- ✅ 写集内存在待提交的业务或必要文档改动
- ✅ 验证档位要求满足（Lite / Standard / High-risk）

2. **状态原子切换**

门禁全过之后，TODO 里的 `[~]` 自动变成 `[x]`，执行实例字段清空，完成证据保留。同一个 Feature 不会被两个会话同时改写，所以并行开发时不需要手工加锁或排队。

3. **Git 收口提醒**

状态更新后会提示主 Agent 精确提交业务改动和唯一 TODO 的 `[x]` hunk。Stop Hook 不执行 `git commit` 或 `git push`，这些操作由主 Agent 在已有授权内完成。

4. **自动续作检查**

同次 commit & push 的远端核验通过后，再在 TODO 里找可执行 Feature。核验前的本地 `[x]` 会处于 `delivery-pending`，不能提前解锁下游。

**续作条件**：

- ✅ 所有依赖的 `[x]` 已进入核验过的远端功能提交
- ✅ 写集与其他进行中任务不冲突
- ✅ 当前开发者是 Owner
- ✅ 当前实例没有其他 `[~]`，候选项没有共享迁移、配置、契约、端口或账号资源

5. **语音提醒**

通知脚本区分确认、任务完成和普通回复。实际触发通道因客户端而异：当前 Claude Code 注册确认事件音频，Codex 完成通知走用户级 `notify`，详见 [中文语音通知](/skills/hooks/notification/)。

<Aside type="tip">
设置 `AGENT_COMPLETION_SOUND=0` 可关闭语音提醒；也可以用下面的环境变量换成自己的音频文件。
</Aside>

## TODO 管理详解

### 当前开发者唯一 TODO

每个开发者只能有一个活动 TODO 文件：

```
docs/<developer-id>/tasks/todo/<项目名>.md
```

**developer-id 获取顺序**：

1. Git `user.name`（`git config user.name`）
2. 系统登录用户名（`whoami`）
3. 环境变量 `AGENT_DEVELOPER_NAME`（最后兜底）

**唯一性检查**：

当前开发者只能有一份活动 TODO。脚本保留历史文件选择逻辑，不能据此并行维护多个活动清单；发现冲突时应先核对 Owner、实例和未完成项，再通过任务维护流程处理。

### Feature 状态机

每个 Feature 使用四种 checkbox 状态；Epic 是汇总节点，不是第五种状态：

```markdown
[ ] - 未开始（依赖未满足或未领取）
[~] - 进行中（已领取，有执行实例）
[x] - 本地已准备完成，远端核验通过后才算已交付
[!] - 阻塞（记录阻塞原因和恢复条件）
Epic - 汇总（不可领取，自动计算）
```

### Feature 结构

完整字段与可复制的格式统一见 [Stop Hook 的 Feature 示例](/skills/hooks/stop/)。解析器从 `验收` 行读取全部 AC，从单行 `结果` 读取证据、来源复核与 Review；独立写 `AC-001` 或 `Review` 行不能替代这些字段。

`规划` 与 `验证` 决定适用门禁，`并行写集` 和 `[~]` 的 `执行实例` 使用反引号值。完成时通过状态机准备 `[x]`，再将该 TODO 改动与完整功能一同 commit、push。

### Epic 汇总

Epic 是同一 TODO 内的完成汇总：

```markdown
- [ ] `USER-MGMT` 用户管理系统
  - [x] `USER-REG` 用户注册
  - [x] `USER-LOGIN` 用户登录
  - [~] `USER-PROFILE` 个人信息
  - [ ] `USER-SETTINGS` 用户设置
```

**特性**：

- ❌ 不可领取（只有 Feature 可以领取）
- ❌ 没有执行实例和写集，不可被领取
- ✅ 最后一个子 Feature 完成时自动变为 `[x]`
- ✅ 用于进度统计和可视化

### 依赖管理

**依赖规则**：

```yaml
合法依赖:
  - 依赖: 无
  - 依赖: `USER-REG`
  - 依赖: `USER-REG`, `USER-LOGIN`
  - 依赖: `USER-MGMT`  # Epic 依赖等价于依赖其全部子 Feature

非法依赖:
  - 依赖: USER-REG  # 缺少反引号
  - 依赖: `UNKNOWN`  # ID 不存在
  - 依赖: `USER-LOGIN`（在 USER-LOGIN 内）  # 自依赖
```

**依赖是否满足**：依赖项必须处于 `[x]`，且与业务改动同次交付并经过远端核验。

| 依赖项状态 | 能否领取 |
|---|---|
| `[x]` 且远端核验通过 | ✓ 可以 |
| 仅本地 `[x]` | ✗ 先完成交付核验 |
| `[~]` 进行中 | ✗ 等它完成 |
| `[ ]` 未开始 | ✗ 先做它 |
| `[!]` 阻塞 | ✗ 先解阻塞 |

**环形依赖**：A 依赖 B、B 依赖 C、C 又依赖 A 这类闭环会被识别并报错，因为闭环里没有任何一项能先开始。

### 并行任务管理

**写集互斥检查**：领取新 Feature 时，把它的写集和所有 `[~]` 任务的写集求交集。交集为空才允许并行。

- 两个 Feature 各改自己的 API 和页面，没有共同文件 → 可以并行
- 两个 Feature 都要改 `src/router/index.ts` → 冲突，应串行，或重新确认共享改动的唯一 Owner 后调整交付边界

**并行条件**：

- ✅ 不同 Feature
- ✅ 不同执行实例
- ✅ 所有依赖已完成远端核验
- ✅ 写集完全互斥（无交集），没有共享迁移、配置、契约、端口或账号资源

### 状态怎么流转

领取、完成、阻塞、释放，以及规划阶段的推进和等待确认，都由 Hook 在后台原子完成，你用自然语言驱动就够了：

| 你说 | 发生什么 |
|---|---|
| "开始实现 USER-LOGIN" | 校验依赖和写集，把它从 `[ ]` 原子领取成 `[~]`，绑定当前会话 |
| "完成 USER-LOGIN" | 走完成门禁；全过才切 `[x]` 并提示提交 |
| "USER-LOGIN 卡住了，等设计稿" | 标 `[!]` 并记下阻塞原因 |
| "继续" / "下一个" | 在 Ready 队列里找下一项并自动领取 |

<Aside type="caution">
状态先落定、再提交：这样 TODO 的更新和代码改动在同一次提交里，不会出现"代码进了远端、状态还留在昨天"。
</Aside>

## 环境变量配置

```bash
# 开发者 ID（仅在 Git 和系统用户名都不可用时）
AGENT_DEVELOPER_NAME=alice-chen

# TODO 目录（仅允许指向当前 Git 根内当前 developer 的规范目录）
AGENT_TODO_DIR=/repo/docs/alice-chen/tasks/todo

# Runtime Instance ID（仅在 Harness 未提供时兜底）
AGENT_RUNTIME_INSTANCE_ID=worker-a

# 停用启动自检、PreToolUse 和提示音；不关闭上下文与 TODO 状态逻辑
AGENT_RUNTIME_HOOKS_DISABLED=1

# 停用 PreToolUse（同时跳过其流程写入门禁）
AGENT_COMMAND_GUARD=0

# 禁用自动修复
AGENT_RUNTIME_HOOKS_AUTO_UPDATE=0

# 禁用语音提醒
AGENT_COMPLETION_SOUND=0

# 自定义音频文件
AGENT_CONFIRMATION_SOUND_FILE=/path/to/confirm.wav
AGENT_REPLY_COMPLETION_SOUND_FILE=/path/to/reply.wav
AGENT_TASK_COMPLETION_SOUND_FILE=/path/to/task.wav

# 启用 Skill 发现兜底（旧客户端）
AGENT_SKILL_DISCOVERY_FALLBACK=1
```

## 故障排查

### Hook 未生效

表现是这一轮完全没有自动上下文：没有行为准则、没有项目记忆、没有 TODO 提醒。

```bash
# 1. 检查 Hook 安装状态
node runtime-hooks/scripts/install.mjs --check --json

# 2. 查看客户端配置
cat ~/.claude/settings.json | grep -A 20 hooks

# 3. 重新安装
node runtime-hooks/scripts/install.mjs
```

Codex 用户装完还要在客户端里运行 `/hooks`，审查并信任新的 Hook——这是 Codex 的安全机制，不能跳过。

### TODO 落在了意外的目录

TODO 的位置由 developer-id 决定，先确认它是什么：

```bash
git config user.name
find docs/ -path "*/tasks/todo/*.md"
```

路径和预期不符时，优先把 `git config user.name` 设成你想要的值，而不是去改文件位置。

### 命令被拦截

先确认这条命令是不是真的该跑。属于误拦截时，唯一的处理方式是你在自己的终端里执行；Agent 侧没有绕过路径。要长期关闭防护，必须在**启动 CLI 之前** `export AGENT_COMMAND_GUARD=0`。

### 并行任务冲突

```bash
# 查看当前进行中的任务，逐个比对写集
grep '\[~\]' docs/<developer-id>/tasks/todo/<项目名>.md
```

有交集时先等冲突任务完成，或明确共享改动的唯一 Owner 后重新规划边界。不要仅从写集里删掉仍然需要修改的文件来规避冲突检查。

<Aside type="tip">
更细的症状-处理对照见 [Hook 故障排查](/skills/hooks/troubleshooting/) 和 [故障排查](/skills/troubleshooting/)。
</Aside>

## 下一步

- [项目记忆](/skills/core-concepts/project-memory/) - 了解项目记忆如何被 Hook 加载
- [功能开发流程](/skills/usage/feature-development/) - 实践完整的开发流程
- [TODO 管理](/skills/usage/todo-management/) - 深入学习 TODO 管理技巧
